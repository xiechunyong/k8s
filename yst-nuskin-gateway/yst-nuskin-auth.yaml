# 外部访问服务
apiVersion: v1
kind: Service
metadata:
  namespace: spring-cloud-demo
  name: yst-nuskin-auth
  labels:
    app: yst-nuskin-auth
spec:
  ports:
    - protocol: TCP
      name: http
      port: 8201
      targetPort: 8201
      nodePort: 8201
  type: NodePort
  selector:
    app: yst-nuskin-auth
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  namespace: spring-cloud-demo
  name: yst-nuskin-auth
spec:
  serviceName: yst-nuskin-auth
  replicas: 1
  template:
    metadata:
      labels:
        app: yst-nuskin-auth
      annotations:
        pod.alpha.kubernetes.io/initialized: "true"
    spec:
      containers:
        - name: yst-nuskin-auth
          imagePullPolicy: IfNotPresent
          image: xiechunyong/yst-nuskin-auth
          resources:
            limits:
              memory: "1Gi"
              cpu: "1"
          ports:
            - containerPort: 8201
              name: client
          env:
            - name: TZ
              value: Asia/Shanghai
            - name: JAVA_OPTS
              value: "-Dnacos=nacos-service:8848 -Dnacos=nacos-service:8848 -Dsentinel=sentinel.spring-cloud-demo:8858"
  selector:
    matchLabels:
      app: yst-nuskin-auth